# Campus Notification System

This is a React-based frontend application that displays campus notifications.

## Features
- Fetch notifications from API
- Show Top 10 based on priority (Placement > Result > Event)
- Filter notifications
- Error handling with fallback data
- Logging middleware integration

## Tech Stack
- React (Vite)
- JavaScript
- Fetch API

## Run Project
npm install
npm run dev
